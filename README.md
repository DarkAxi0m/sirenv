# sirenv
Person Env to setup my workspaces

## External Projects

- `matrixstatus` now lives in `~/sandbox/matrixstatus`.

## Maintenance

- Refresh cached `mmctl` bash completion with `scripts/refresh-mmctl-completion`.
